server:
  port: 8222

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      server:
        webmvc:
          routes-map:
            user-service-route:
              uri: lb://user-service
              predicates:
                - Path=/api/users/**